/** Reusable diagonal-stripe divider — the brand's hazard-tape motif, used sparingly between sections. */
export default function HazardDivider() {
  return <div className="caution-divider" aria-hidden="true" />
}
